# Historical build plans — do not follow for the slim starter

These notes predate the slim keep set. They describe a resume-builder / nonprofit-CMS path and may mention `OPENROUTER_API_KEY_REACHTHEMAI` or Marketing Agent env.

**Do not treat them as required setup.**

- Documents live at `/documents` (not `/app/resume-builder`).
- Use `OPENROUTER_API_KEY` or per-user BYOK. Do **not** set `OPENROUTER_API_KEY_REACHTHEMAI`.
- Do not copy Marketing Agent project IDs (`glplvrljdgowcwuubkau`, `marketing-agent-truman`).
- Composio Ads/SEO catalogs are out of v1.

Canonical fork docs: repository root [`README.md`](../../../README.md) and [`docs/plans/slim-starter-feature-extract.md`](../../../docs/plans/slim-starter-feature-extract.md).
