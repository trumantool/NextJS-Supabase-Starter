export const metadata = {
  title: 'Skills Library',
  description: 'Shared and personal agent skill files.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
